"""Fourth page, kept simple for this first project part."""

import streamlit as st


st.title("Notes")
# The assignment only asks for test content on page four at this stage.
st.write(
    "This first version reads a CSV stored with the app. In project part 2, "
    "the data source will change to MongoDB. For now I am keeping this page "
    "as a small placeholder, as the assignment asks."
)
